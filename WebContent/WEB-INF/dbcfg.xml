<?xml version="1.0" encoding="UTF-8"?>
<root>
	<connection>
		<driver>com.mysql.cj.jdbc.Driver</driver>
		<url>jdbc:mysql://localhost:3306/scuola?useSSL=false</url>
		<user>root</user>
		<password></password>
	</connection>
	<query>
		<insertStudente>INSERT INTO studenti(email, password, nome, cognome, nascita, classe) VALUES(?,?,?,?,?,?);</insertStudente>
		<insertDocente>INSERT INTO docenti(email, password, nome, cognome) VALUES(?,?,?,?);</insertDocente>
		<checkAmministratore>SELECT 1 FROM amministratori WHERE email = ? and password = ?;</checkAmministratore>
		<checkStudenti>SELECT 1 FROM studenti WHERE email = ? and password = ?;</checkStudenti>
		<checkDocenti>SELECT 1 FROM docenti WHERE email = ? and password = ?;</checkDocenti>
	</query>
</root>